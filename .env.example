# Database — used by config/database.yml for local/docker-compose.
# Production typically uses DATABASE_URL set by the deploy platform.
DB_HOST=127.0.0.1
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
