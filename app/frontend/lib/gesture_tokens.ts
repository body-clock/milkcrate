/** Gesture thresholds and durations — single source of truth for touch interactions. */

/** Minimum pointer movement (px) before a swipe is recognized. */
export const SWIPE_THRESHOLD = 72

/** Hold duration (ms) before a long-press is recognized. */
export const LONG_PRESS_DURATION = 500

/** Maximum pointer movement (px) during a tap before it's classified as a drag. */
export const TAP_MOVE_THRESHOLD = 10

/** Pull distance (px) at which a pull-to-action fires. */
export const PULL_THRESHOLD = 100
